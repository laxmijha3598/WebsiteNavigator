# Production / Vercel: set this to your deployed API base URL (https, no trailing slash).
# Add the same name in Vercel: Project → Settings → Environment Variables.
VITE_API_URL=https://websitenavigator-1.onrender.com
